import { Routes } from '@angular/router';
import { DrinkTestComponent } from './drink-test/drink-test.component';

export const routes: Routes = [
  {
    path: '',
    component: DrinkTestComponent,
  }
];